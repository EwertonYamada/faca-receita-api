spring:
  application:
    name: faca-receita

  datasource:
    url: jdbc:postgresql://localhost:5436/facareceita
    username: facareceita
    password: facareceita
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect

server:
  port: 8080

logging:
  level:
    org:
      springframework: INFO
    com:
      faca_receita: DEBUG

jwt:
  secret: O18IsyskWGYWW7DEnJKS9IN48fZCUYtDS8ccMVFPkmQ=